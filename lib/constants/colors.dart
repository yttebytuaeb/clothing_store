import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const background = Color(0xFF10002B);
  static const surface = Color(0xFF240046);
  static const primary = Color(0xFF3C096C);
  static const secondary = Color(0xFF5A189A);

  // Accent Colors
  static const accent = Color(0xFF7B2CBF);
  static const highlight = Color(0xFF9D4EDD);

  // Text Colors
  static const textPrimary = Color(0xFFE0AAFF);
  static const textSecondary = Color(0xFFC77DFF);

  // Additional UI Colors
  static const error = Color(0xFFFF6B6B);
  static const success = Color(0xFF4CD964);
  static const cardBackground = Color(0xFF240046);
  static const divider = Color(0xFF3C096C);

  // Compatibility colors (to replace removed colors)
  static const white = Color(0xFFFFFFFF);
  static const darkBlue = Color(0xFF1A1A2E);
  static const mediumBlue = Color(0xFF16213E);
  static const lightBlue = Color(0xFF0F3460);
  static const darkNavy = Color(0xFF0D1B2A);
  static const deepBlue = Color(0xFF1B263B);
}
